import React from "react";

/**
 * Dashboard page — stateless for now, uses mocked data.
 * Replace mockedData with API calls when backend is ready.
 */
const Dashboard = () => {
  // Mocked stats (replace with real API data)

  return (
    <div className="m-auto text-center flex items-center justify-center min-h-screen">
      <div className="text-center">
        <h1 className="text-3xl font-semibold mb-2">Dashboard</h1>
        <p className="text-gray-600 text-lg">
          Dashboard analytics will be added soon....
        </p>
      </div>
    </div>
  );
};

export default Dashboard;
